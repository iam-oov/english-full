# Skill Registry — english-full

<!-- Auto-generated by gentle-ai skill-registry refresh. Run `gentle-ai skill-registry refresh --force` to regenerate. -->

Last updated: 2026-06-10

## Sources scanned

- /home/valdo/.agents/skills
- /home/valdo/.claude/skills
- /home/valdo/.codex/skills

## Contract

**Delegator use only.** This registry is an index, not a summary. Any agent that launches subagents reads it to select relevant skills, then passes exact `SKILL.md` paths for the subagent to read before work.

`SKILL.md` remains the source of truth. Do not inject generated summaries or compact rules by default; pass paths so subagents load the full runtime contract and preserve author intent.

## Skills

| Skill | Trigger / description | Scope | Path |
| --- | --- | --- | --- |
| `adr-cleanup` | Limpiar, normalizar y traducir documentación ADR de un agente Tiriel. Usa esta skill cuando el usuario mencione limpiar ADR, normalizar documentación de agente, estructurar carpeta de producto, limpiar export de DocMost, o agregar un nuevo ADR/ARD al workspace. También actívala si el usuario dice "limpiar docs", "normalizar carpeta", "nuevo agente ADR", o si detectas una carpeta con prefijo ARD_ o ADR_ que contenga archivos .md desestructurados (directorios espurios, slashes en nombres, sin estructura canónica). | user | `/home/valdo/.claude/skills/adr-cleanup/SKILL.md` |
| `architecture-patterns` | Implement proven backend architecture patterns including Clean Architecture, Hexagonal Architecture, and Domain-Driven Design. Use this skill when designing clean architecture for a new microservice, when refactoring a monolith to use bounded contexts, when implementing hexagonal or onion architecture patterns, or when debugging dependency cycles between application layers. | user | `/home/valdo/.agents/skills/architecture-patterns/SKILL.md` |
| `branch-pr` | Create Gentle AI pull requests with issue-first checks. Trigger: creating, opening, or preparing PRs for review. | user | `/home/valdo/.claude/skills/branch-pr/SKILL.md` |
| `chained-pr` | Trigger: PRs over 400 lines, stacked PRs, review slices. Split oversized changes into chained PRs that protect review focus. | user | `/home/valdo/.claude/skills/chained-pr/SKILL.md` |
| `cognitive-doc-design` | Design docs that reduce cognitive load. Trigger: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs. | user | `/home/valdo/.claude/skills/cognitive-doc-design/SKILL.md` |
| `comment-writer` | Write warm, direct collaboration comments. Trigger: PR feedback, issue replies, reviews, Slack messages, or GitHub comments. | user | `/home/valdo/.claude/skills/comment-writer/SKILL.md` |
| `find-skills` | Helps users discover and install agent skills when they ask questions like "how do I do X", "find a skill for X", "is there a skill that can...", or express interest in extending capabilities. This skill should be used when the user is looking for functionality that might exist as an installable skill. | user | `/home/valdo/.agents/skills/find-skills/SKILL.md` |
| `go-testing` | Trigger: Go tests, go test coverage, Bubbletea teatest, golden files. Apply focused Go testing patterns. | user | `/home/valdo/.claude/skills/go-testing/SKILL.md` |
| `imagegen` | Generate or edit raster images when the task benefits from AI-created bitmap visuals such as photos, illustrations, textures, sprites, mockups, or transparent-background cutouts. Use when Codex should create a brand-new image, transform an existing image, or derive visual variants from references, and the output should be a bitmap asset rather than repo-native code or vector. Do not use when the task is better handled by editing existing SVG/vector/code-native assets, extending an established icon or logo system, or building the visual directly in HTML/CSS/canvas. | user | `/home/valdo/.codex/skills/.system/imagegen/SKILL.md` |
| `issue-creation` | Create Gentle AI issues with issue-first checks. Trigger: creating GitHub issues, bug reports, or feature requests. | user | `/home/valdo/.claude/skills/issue-creation/SKILL.md` |
| `judgment-day` | Trigger: judgment day, dual review, adversarial review, juzgar. Run blind dual review, fix confirmed issues, then re-judge. | user | `/home/valdo/.claude/skills/judgment-day/SKILL.md` |
| `memory-audit` | Audit the health of the Tiriel .memory/ system across all repos. Scans every repo's .memory/STATE.md, regenerates the full WORKSPACE.md Quick Map table, detects stale STATE.md files (>14 days), and validates CONTRACTS.md boundary paths. Use this skill for periodic maintenance, when the user says "audit memory", "check memory health", "memory-audit", "which repos are stale?", "is the workspace up to date?", "refresh workspace table", "regenerate workspace", or any request to verify the overall state of the .memory/ system. This is a read-heavy maintenance tool, not a session save (use memory-sync for that). | user | `/home/valdo/.claude/skills/memory-audit/SKILL.md` |
| `memory-start` | Load context from the Tiriel .memory/ system based on the current working directory. When executed from the workspace root, loads the full cross-repo context (tiriel-planning/ global files + all per-repo STATE.md summaries). When executed from inside a specific repo, loads only that repo's .memory/ files (STATE.md, DECISIONS.md, ROADMAP.md, CONTRACTS.md depending on tier). Use this skill at the start of any session, when switching repos, when the user says "load memory", "load context", "what's the current state?", "catch me up", "where were we?", "what am I working on?", or any request to orient in the workspace or a specific repo. Also trigger on "memory-start", "start session", "load .memory", or "read state". This is the recommended first action in any new conversation about the Tiriel codebase. | user | `/home/valdo/.claude/skills/memory-start/SKILL.md` |
| `memory-sync` | Automatically save the current session's work to the Tiriel .memory/ system. Analyzes what was done (git changes, decisions, patterns, state changes) and writes directly to the correct .memory/ files without asking the user — this is a delegated task. The user trusts the system to know which file each piece of information belongs in. Use this skill when the user says "save memory", "sync memory", "memory-sync", "persist session", "save what we did", "update memory", "guard memory", or at the end of any significant working session. Also trigger when the user says "let's save this", "we're done, save everything", or "wrap up the session". Do NOT trigger for loading context (use memory-load) or for milestone completion (use memory-work). This skill is specifically for persisting session learnings. | user | `/home/valdo/.claude/skills/memory-sync/SKILL.md` |
| `memory-work` | Complete a milestone in the Tiriel .memory/ system. Captures what was accomplished in a body of work, gathers git stats, archives the milestone, updates STATE.md and ROADMAP.md, writes a retrospective, and tags the release in git. Adapted from GSD's complete-milestone workflow for multi-repo microservices. Use this skill when the user says "complete milestone", "mark milestone", "wrap up this phase", "ship it", "milestone done", "close milestone", "archive milestone", "memory-work", "what did we accomplish?", or wants to formally record the completion of a significant body of work. Also trigger when the user wants to create a retrospective, review what was built, or prepare for the next phase of development. Do NOT trigger for simple end-of-session updates — those are just STATE.md edits. This skill is for significant completions: a phase shipped, an MVP done, a major refactor landed. | user | `/home/valdo/.claude/skills/memory-work/SKILL.md` |
| `new-celery-worker` | Genera la estructura base completa de un nuevo Celery worker para el ecosistema Tiriel, siguiendo las reglas canonicas del workspace: screaming architecture, config plano con pydantic-settings, composition root sin DI framework, services/ con ports y adapters co-localizados, use_cases/ para logica de negocio, y task delgado con cola configurada lista para recibir payloads. Usa este skill cuando el usuario pida crear un nuevo servicio, worker, agente, o repo de Celery. Tambien activalo si menciona "scaffolding", "nuevo proyecto", "crear repo", "estructura base", o "boilerplate". | user | `/home/valdo/.claude/skills/new-celery-worker/SKILL.md` |
| `openai-docs` | Use when the user asks how to build with OpenAI products or APIs, asks about Codex itself or choosing Codex surfaces, needs up-to-date official documentation with citations, help choosing the latest model for a use case, or model upgrade and prompt-upgrade guidance; use OpenAI docs MCP tools for non-Codex docs questions, use the Codex manual helper first for broad Codex self-knowledge, and restrict fallback browsing to official OpenAI domains. | user | `/home/valdo/.codex/skills/.system/openai-docs/SKILL.md` |
| `pdf` | Use this skill whenever the user wants to do anything with PDF files. This includes reading or extracting text/tables from PDFs, combining or merging multiple PDFs into one, splitting PDFs apart, rotating pages, adding watermarks, creating new PDFs, filling PDF forms, encrypting/decrypting PDFs, extracting images, and OCR on scanned PDFs to make them searchable. If the user mentions a .pdf file or asks to produce one, use this skill. | user | `/home/valdo/.agents/skills/pdf/SKILL.md` |
| `plugin-creator` | Create and scaffold plugin directories for Codex with a required `.codex-plugin/plugin.json`, optional plugin folders/files, valid manifest defaults, and personal-marketplace entries by default. Use when Codex needs to create a new personal plugin, add optional plugin structure, generate or update marketplace entries for plugin ordering and availability metadata, or update an existing local plugin during development with the CLI-driven cachebuster and reinstall flow. | user | `/home/valdo/.codex/skills/.system/plugin-creator/SKILL.md` |
| `repo-architecture-doc` | Genera y actualiza un archivo ARCHITECTURE.md que documenta la arquitectura de un workspace multi-repo: objetivo de cada repo, cómo interactúan entre sí, reglas específicas, e infraestructura compartida. Usa este skill cuando el usuario pida documentar la arquitectura, mapear repos, entender cómo se conectan los servicios, actualizar el mapa de arquitectura, o cuando mencione ARCHITECTURE.md. También actívalo si el usuario pregunta "cómo se relacionan los repos", "qué hace cada servicio", o pide un overview del sistema. | user | `/home/valdo/.claude/skills/repo-architecture-doc/SKILL.md` |
| `skill-creator` | Trigger: new skills, agent instructions, documenting AI usage patterns. Create LLM-first skills with valid frontmatter. | user | `/home/valdo/.claude/skills/skill-creator/SKILL.md` |
| `skill-improver` | Trigger: improve skills, audit skills, refactor skills, skill quality. Audit and upgrade existing LLM-first skills. | user | `/home/valdo/.claude/skills/skill-improver/SKILL.md` |
| `skill-installer` | Install Codex skills into $CODEX_HOME/skills from a curated list or a GitHub repo path. Use when a user asks to list installable skills, install a curated skill, or install a skill from another repo (including private repos). | user | `/home/valdo/.codex/skills/.system/skill-installer/SKILL.md` |
| `sync-merge` | Sync the current feature branch with its base branch (default develop) by merging the latest REMOTE base in, then repair merge conflicts and any tests the merge broke — using judgment and the live session context, never blind auto-resolution. Runs tests via the repo's Docker container, never locally. Guards a clean working tree before touching anything. Never commits or pushes — leaves that to the human. Trigger: When the user wants to bring base-branch changes into their branch — "merge develop", "mergear develop", "sincronizar rama", "actualizar mi rama", "traer los cambios de develop", "sync branch", "update my branch with develop", "merge the base branch". | user | `/home/valdo/.claude/skills/sync-merge/SKILL.md` |
| `tiriel-a2-e2e-delay` | Runs the Tiriel A2 (load matching) end-to-end test via the ONE-SHOT delay endpoint: POST /delays → RedBeat → A2S → A2 pipeline logs. Sister skill: tiriel-a2-e2e-schedule (recurring crontab entry point). Trigger: When user asks to run the A2 e2e via delay, "e2e de a2 por delay", "prueba e2e one-shot", "checar si A2 funciona", or validate the A2S → A2 flow with a single execution. If the user says "schedule" or "crontab", use tiriel-a2-e2e-schedule instead. | user | `/home/valdo/.claude/skills/tiriel-a2-e2e-delay/SKILL.md` |
| `tiriel-a2-e2e-schedule` | Runs the Tiriel A2 (load matching) end-to-end test via the RECURRING schedule endpoint: POST /schedules (crontab) → RedBeat recurring entry → A2S → A2 pipeline logs, then MANDATORY cleanup (DELETE the schedule). Sister skill: tiriel-a2-e2e-delay (one-shot). Trigger: When user asks to run the A2 e2e via schedule/crontab, "e2e de a2 por schedule", "probar el flujo recurrente de load matching", or validate the RedBeat crontab → A2S → A2 flow. If the user says "delay" or one-shot, use tiriel-a2-e2e-delay instead. | user | `/home/valdo/.claude/skills/tiriel-a2-e2e-schedule/SKILL.md` |
| `work-unit-commits` | Plan commits as reviewable work units. Trigger: implementation, commit splitting, chained PRs, or keeping tests and docs with code. | user | `/home/valdo/.claude/skills/work-unit-commits/SKILL.md` |

## Loading protocol

1. Match task context and target files against the `Trigger / description` column.
2. Pass only the matching `Path` values to the subagent under `## Skills to load before work`.
3. Instruct the subagent to read those exact `SKILL.md` files before reading, writing, reviewing, testing, or creating artifacts.
4. If no matching skill exists, proceed without project skill injection and report `skill_resolution: none`.
