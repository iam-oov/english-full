node_modules/
dist/
.astro/

# El .gitignore de la raíz (plantilla Python) ignora `lib/` como artefacto de
# build; acá src/lib/ es CÓDIGO FUENTE (los puertos del juego) -> re-incluir.
!src/lib/
playwright-report/
test-results/
